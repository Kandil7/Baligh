[project]
name = "baligh"
version = "0.2.0"
description = "Baligh-1.7B: Arabic-first LLM with Islamic knowledge specialization"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "Apache-2.0"}
authors = [
    {name = "Baligh Team", email = "team@baligh.ai"}
]
keywords = [
    "llm",
    "arabic",
    "islamic",
    "qwen3",
    "continued-pretraining",
    "instruction-tuning",
    "unsloth",
    "qlora"
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "torch>=2.6.0,<3",
    "transformers>=4.51.0,<5",
    "accelerate>=0.37.0",
    "peft>=0.14.0",
    "datasets>=2.21.0",
    "huggingface-hub>=0.26.0",
    "bitsandbytes>=0.44.0",
    "pydantic>=2.9.0",
    "pydantic-settings>=2.5.0",
    "pyyaml>=6.0",
    "rich>=13.7.0",
    "loguru>=0.7.0",
    "wandb>=0.18.0",
    "tensorboard>=2.17.0",
    "numpy>=1.26.0",
    "pandas>=2.2.0",
    "pyarrow>=16.0.0",
    "tqdm>=4.66.0",
    "python-dotenv>=1.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "pytest-cov>=5.0.0",
    "pytest-xdist>=3.5.0",
    "pytest-mock>=3.14.0",
    "ruff>=0.6.0",
    "black>=24.10.0",
    "mypy>=1.11.0",
    "pyright>=1.1.380",
    "pre-commit>=3.8.0",
    "isort>=5.13.0",
    "types-PyYAML>=6.0.0",
    "types-tqdm>=4.66.0",
]
training = [
    "deepspeed>=0.15.0",
    "flash-attn>=2.6.0",
    "xformers>=0.0.28",
]
eval = [
    "evaluate>=0.4.3",
    "rouge-score>=0.1.2",
    "bert-score>=0.3.13",
    "sacrebleu>=2.4.0",
    "nltk>=3.8.1",
    "arabic-reshaper>=3.0.0",
    "python-bidi>=0.4.2",
]
deployment = [
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
    "pydantic>=2.9.0",
]
docs = [
    "mkdocs>=1.6.0",
    "mkdocs-material>=9.5.0",
    "mkdocstrings[python]>=0.26.0",
]

[tool.black]
line-length = 100
target-version = ['py311', 'py312']
include = '\.pyi?$'
extend-exclude = '''
/(
  \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | _build
  | buck-out
  | build
  | dist
  | training
  | eval
  | release
)/
'''

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "T20", # flake8-print
    "SIM", # flake8-simplify
    "ARG", # flake8-unused-arguments
]
ignore = [
    "E501",  # line too long (handled by black)
    "B008",  # do not use mutable data structures for defaults
    "C408",  # do not use list comprehension for side effects
]
[tool.ruff.lint.per-file-ignores]
# Tests: mock conventions use uppercase names, fixtures are intentionally
# accepted-but-unused, and results are often asserted via mock calls.
"tests/*" = ["S101", "S106", "N803", "N806", "ARG001", "ARG002", "F841", "SIM117"]
"src/scripts/*" = ["T201"]
"colab_cli/*" = ["T201"]
# HF TrainerCallback methods receive a fixed argument signature; unused
# args/state/control are unavoidable there.
"src/baligh/training/callbacks.py" = ["ARG002"]
# seed_worker(worker_id) matches torch DataLoader's required signature.
"src/baligh/utils/seeding.py" = ["ARG001"]
# Modal functions are CLI entry points; printing IS the interface.
"src/modal/*" = ["T201"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.mypy]
# Parse target 3.12: numpy>=2.x stubs use PEP 695 syntax that fails to parse
# at 3.11. Our own code remains 3.11-compatible (verified by CI on 3.11).
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
no_implicit_optional = true
strict_optional = true
ignore_missing_imports = false
namespace_packages = true
explicit_package_bases = true
mypy_path = "src"
exclude = ["src/modal", "colab_cli"]

[[tool.mypy.overrides]]
# Third-party libs without stubs or py.typed markers.
module = [
    "datasets.*",
    "tqdm.*",
    "bert_score.*",
    "datasketch.*",
    "autoawq.*",
    "auto_gptq.*",
    "sacrebleu.*",
]
ignore_missing_imports = true

[[tool.mypy.overrides]]
# numpy>=2 stubs use PEP 695 `type` statements, unparseable when mypy
# targets 3.11. Skip analyzing them; our usage is typed at call sites.
module = ["numpy.*"]
follow_imports = "skip"
ignore_errors = true

[[tool.mypy.overrides]]
# CLI entry points: argparse glue, annotations are low-value there.
module = ["scripts.*"]
disallow_untyped_defs = false
disallow_incomplete_defs = false

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "--strict-markers",
    "--strict-config",
    "--cov=src/baligh",
    "--cov-report=term-missing",
    "--cov-report=html:htmlcov",
    "--cov-fail-under=80",
    "-v",
]
markers = [
    "unit: Unit tests",
    "integration: Integration tests",
    "slow: Slow tests",
    "gpu: Tests requiring GPU",
]

[tool.isort]
profile = "black"
line_length = 100
multi_line_output = 3
include_trailing_comma = true
force_grid_wrap = 0
use_parentheses = true
ensure_newline_before_comments = true
src_paths = ["src"]

[tool.coverage.run]
source = ["src/baligh"]
omit = [
    "*/tests/*",
    "*/scripts/*",
    "*/notebooks/*",
    "*/__main__.py",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
]

[build-system]
requires = ["setuptools>=70.0.0", "wheel"]
build-backend = "setuptools.build_meta"