# Base requirements for Baligh-1.7B v0
# Core ML stack
# torch >= 2.6.0: CVE-2025-32434 (torch.load weights_only bypass RCE) fixed in 2.6
torch>=2.6.0,<3
# transformers >= 4.51.0: Qwen3 architecture support required by the base model
transformers>=4.51.0,<5
accelerate>=0.37.0
peft>=0.14.0
datasets>=2.21.0
huggingface-hub>=0.26.0

# Quantization
bitsandbytes>=0.44.0

# Configuration & utilities
pydantic>=2.9.0
pydantic-settings>=2.5.0
pyyaml>=6.0
rich>=13.7.0
loguru>=0.7.0
python-dotenv>=1.0.0

# Data processing
numpy>=1.26.0
pandas>=2.2.0
pyarrow>=16.0.0
tqdm>=4.66.0

# Experiment tracking
wandb>=0.18.0
tensorboard>=2.17.0
