# Review: evaluation + inference + scripts + colab_cli (v0.1.0)

### Context

Baligh repo (D:\AI\Projects\LLM\Baligh), Arabic LLM fine-tuning (Qwen3-1.7B QLoRA, CPT+SFT, Colab T4 target). Read-only review of src/baligh/evaluation/*, src/baligh/inference/*, src/scripts/*, colab_cli/run.py, tests/conftest.py requested by main agent. Follow-up to review-training-checkpoint-core.md.

### Explanation

CRITICAL: (1) merge_lora.py loads base WITHOUT adapter then calls merge_and_unload() on plain model -> AttributeError; --adapter-path parsed/logged but never used. (2) colab_cli/run.py:55 unterminated f-string -> SyntaxError, tool cannot run. (3) metrics.compute_rouge uses rouge_score default tokenizer ([a-z0-9]+ after lowercase) which drops ALL Arabic tokens -> ROUGE ~0 regardless of quality; arabic-reshaper/python-bidi installed in [eval] extras but never imported. (4) data/loader.DATASET_REGISTRY has only 3 CPT entries vs datasets.DATASETS 14 -> load_sft_datasets/load_eval_datasets raise Unknown dataset -> prepare_data sft/eval stages and run_eval crash. (5) train_sft(base_model_path=...) accepted but never threaded into _setup_model (always unsloth base) -> CPT->SFT chain silently discards CPT. MAJOR: generator/evaluator use `temperature or config.temperature` so temperature=0 impossible; EvalConfig defaults do_sample=True+temp0.7 with no seed in run_eval -> irreproducible benchmarks; MMLU_Arabic runner assumes choices/answer dict fields that do not exist in FreedomIntelligence/MMLU_Arabic (HF viewer shows cast error; columns are positional 0-3 + letter) and takes response.strip()[0] which breaks when reply starts with Arabic letter; ChatBot has no history truncation; custom ChatML fallback template in models/tokenizer.py is invalid Jinja ({{% -> TemplateSyntaxError, verified locally) though latent because Qwen3-1.7B base ships a chat_template; formatter tokenize=True branch never truncates and copies full labels (response_only_loss config is dead); models/loader disables KV cache + runs prepare_model_for_kbit_training on inference paths; flash_attention_2 hardcoded (T4 sm_75 unsupported); deduplicate_dataset calls len()/select() on IterableDataset -> --deduplicate crashes streaming CPT; validate_dataset does a full extra streaming pass and results are never used to filter; islamic_qa_custom absent from registry so run_eval silently skips Islamic QA; reporters nested-quote f-strings are SyntaxError on declared Python 3.11; human_eval export_csv aligns scores.values() against rubric header (misalignment when key sets differ) and get_average_scores KeyErrors on heterogeneous keys; exact match has zero Arabic normalization. MINOR: perplexity weighted by sequences not tokens and returns inf when avg_loss==0; BLEU without sacrebleu tokenize='ar'; structured.py has no markdown-fence stripping and dead schema placeholder; cleaner \s+->space destroys newlines/document structure (incl. Islamic texts), diacritics regex dead, control-chars regex mangled, punctuation filters English-only; load_dataset_by_name splats metadata keys into load_dataset kwargs; colab_cli hardcodes Kandil7/Baligh@develop URLs and --repo arg unused; model card claims 32k context vs 2048 training. TESTS: all in-scope modules have files but they are mock call-throughs asserting dict membership/isinstance-str; mocked dataset schemas encode wrong assumptions (choices dict); decode mocked to return str makes assertions tautological; 8 src modules have no test file (constants, data/datasets, data/loader, models/loader, models/quantization, cpt_trainer, sft_trainer, utils/distributed); conftest is 3 fixtures. DONE WELL: correct new-token-only decode slicing; ensure_ascii=False UTF-8 everywhere; clean package separation + frozen dataclass configs; seeding plumbed end-to-end; graceful datasketch fallback; central pad=eos handling. VERDICT: REWORK - metrics invalid for Arabic, three broken stage handoffs, non-deterministic eval.

### Next Steps

Builder should: (1) replace ROUGE tokenizer with Arabic-aware tokenization (pre-tokenize with Farasa/CAMeL or simple alphanumeric+\u0600-\u06FF regex, feed tokenized strings), add Arabic normalization to EM, sacrebleu tokenize='ar'; (2) fix merge_lora to PeftModel.from_pretrained(base, adapter) then merge_and_unload in fp16/bf16 and save tokenizer; (3) unify dataset registries and thread base_model_path; (4) switch `or` defaults to `if x is None`; force do_sample=False/temperature=None for benchmarks + set seed; (5) rewrite MMLU runner against real schema with regex letter extraction and instruction to answer with a letter; (6) add history truncation window to ChatBot; delete or fix Jinja fallback template; enforce max_seq_length truncation in formatter tokenize branch and implement response_only_loss masking; (7) gate FA2/bf16 behind GPU-capability check with sdpa/fp16 T4 profile; (8) make dedup/validation streaming-compatible; register islamic_qa_custom or drop from eval config; (9) fix run.py f-strings; (10) rewrite tests to assert behavior (accuracy value, generate kwargs, slice correctness) with realistic Arabic fixtures.

---
